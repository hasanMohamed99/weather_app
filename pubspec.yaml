name: pearogram_task
description: Pearogram Flutter task.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  equatable:
  flutter_bloc:
  bloc:
  get_it:
  intl:
  dio:
  animate_do:
  device_preview:
  geolocator:
  lottie:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
